# Database - SQLAlchemy ORM
sqlalchemy>=2.0.23
geoalchemy2>=0.14.3
alembic>=1.13.0  # Database migrations
psycopg2-binary>=2.9.9
# Note: psycopg2-binary is the stand-alone version (easier to install)
# For production, consider psycopg2 (requires PostgreSQL client libraries)

# Web scraping (if not already installed)
cloudscraper>=1.2.71
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Utilities
python-dotenv>=1.0.0  # For loading .env configuration files
